# Nixpacks configuration for Railway
# Railway usa Nixpacks para detectar e buildar projetos

[phases.setup]
nixPkgs = ["nodejs_20", "postgresql_16"]

[phases.install]
cmds = [
  "npm ci"
]

[phases.build]
cmds = [
  "npx prisma generate",
  "npm run build"
]

[start]
cmd = "npm start"
